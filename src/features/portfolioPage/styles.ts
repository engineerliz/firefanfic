import { css } from '@emotion/css';

export const portfolioPageStyles = {
  portfolioHeaderContainer: css({
    justifyContent: 'space-between',
  }),
  portfolioHeader: css({
    marginBottom: 12,
  })
}
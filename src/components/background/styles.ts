import { css } from '@emotion/css';

export const backgroundStyles = {
  container: css({
    position: 'fixed',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
  }),
  backgroundImg: css({
    objectFit: 'cover',
    width: '100%',
  })
}